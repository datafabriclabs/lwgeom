use crate::{GBox, LWGeom, LWGeomRef, Result};

impl LWGeom {
    pub fn mvt_geom(
        geom: &LWGeomRef, bounds: &GBox, extent: u32, buffer: u32, clip_geom: bool,
    ) -> Result<Self> {
        todo!()
    }
}
